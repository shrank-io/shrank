[project]
name = "shrank-inference"
version = "0.1.0"
description = "vllm-mlx launcher for Shrank — runs Gemma 4 on Apple Silicon"
requires-python = ">=3.11"
dependencies = [
    "vllm-mlx>=0.2.8",
]
